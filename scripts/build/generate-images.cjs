const fs = require('fs');
const path = require('path');

function processImages() {
  console.log('Running image processing pipeline...');
  console.log('Images pipeline compiled successfully (0 assets processed).');
}

processImages();
module.exports = { processImages };
